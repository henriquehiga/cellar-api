services:
  app:
    restart: always
    ports:
      - "3000:4000"
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"